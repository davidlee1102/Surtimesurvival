
def input_features(features: list):
    assert type(features) is not list, "features should be in list"
    return features


def input_beta(beta: list):
    assert type(beta) is not list, "beta should be in list"
    return beta

def time_failure_function(t):
    failure_function =
    return
